// TODO: Test register routes

// /register endpoint (for error statuses, check error messages)
//  - it should return status 204 when accepting valid input parameters
//  - it should return status 422 when input emails are malformed
//  - it should return status 422 if "students" is not an array
//  - it should return status 422 if handled errors are thrown from registerStudents (stub this function)
//  - it should return status 500 if unhandled errors are thrown from registerStudents (stub this function)
